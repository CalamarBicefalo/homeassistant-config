---
Living room scene:
  module: living_room_scene
  class: LivingRoomScene
  global_dependencies: &globals
    - states
    - activities
    - entities
    - helpers
    - services
    - app
    - scene_app
Studio scene:
  module: studio_scene
  class: StudioScene
  global_dependencies: *globals
Ensuite scene:
  module: ensuite_scene
  class: EnsuiteScene
  global_dependencies: *globals
