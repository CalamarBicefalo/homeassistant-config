# function to find all entities in a group.
# A group worth trying is "group.all"
# def check_group(self, entity, group):
#     group = self.get_state(group, attribute = "all")
#     return entity in group["attributes"]["entity_id"]

# it will return true if the entity is in the group, false otherwise.
# self.log(self.check_group("light.office_4", "group.upstairs_lights"))
